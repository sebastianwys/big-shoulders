# pipeline dependencies for the repo root. the modeling work in ml/ has its own requirements.txt
# pinned to the versions that reproduce every committed artifact byte for byte on python 3.12.
# loosen deliberately, then re-check the hashes in the readme
matplotlib==3.10.9
numpy==2.4.4
openpyxl==3.1.5
pandas==3.0.2
requests==2.33.1
seaborn==0.13.2
snakemake==9.20.0
